# gdedit v0.3.0

## Highlights

- Editing-first terminal workflow with a more usable Control Hub.
- Slash file commands: `/open`, `/write`, `/saveas`, `/sync`.
- Per-file Control Hub conversation history with `F3` popup.
- Reply preview truncation in the main UI while full wrapped history remains available in the popup.
- Better Control Hub text editing: delete on selection, word movement/selection, safer Alt handling.
- Memo capture improvements:
  - explicit `->` payload support
  - `-.` shorthand for multi-line memo bullets
  - app/project/system memo scope routing
  - app memos stored at `memoRoot/app/[app-name].md`
- Generic process sync improvements:
  - `--sync-register`, `--sync-list`, `--sync`, `--sync-remove`
  - sync registry stored at `~/.config/gdedit/process_sync.json`
  - sync-backed buffers save full text through stdin

## Expected Release Assets

- `gdedit_0.3.0_windows_amd64.zip`
- `gdedit_0.3.0_windows_arm64.zip`
- `gdedit_0.3.0_darwin_amd64.tar.gz`
- `gdedit_0.3.0_darwin_arm64.tar.gz`
- `gdedit_0.3.0_linux_amd64.tar.gz`
- `gdedit_0.3.0_linux_arm64.tar.gz`
- `gdedit_0.3.0_checksums.txt`

## Release Notes Draft

gdedit v0.3.0 improves the editing-first terminal workflow with a friendlier onboarding flow,
better Control Hub editing, per-file history, clearer memo handling, and a generic process sync system.

Major user-facing changes:

- clearer CLI help and Quick Start documentation
- open/save file commands directly from the Control Hub
- wrapped file-specific history popup via `F3`
- deterministic memo capture from one-line input
- generic subprocess sync registration, listing, opening, and removal
